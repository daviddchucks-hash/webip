kind = "web"
previewPath = "/"
title = "WebIP - IP & Website Inspector"
version = "1.0.0"
id = "artifacts/webip"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18150

[services.development]
run = "pnpm --filter @workspace/webip run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/webip", "run", "build" ]
serve = "static"
publicDir = "artifacts/webip/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18150"
BASE_PATH = "/"
